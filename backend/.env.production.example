# RideX LIVE/PRODUCTION environment only — never commit real values
RIDEX_ENVIRONMENT=LIVE
NODE_ENV=production
DATABASE_URL_LIVE=postgresql://USER:PASSWORD@HOST:5432/ridex_live
# Admin production network allowlist (comma-separated IP/CIDR forms supported by middleware)
RIDEX_ADMIN_ALLOWED_IPS=203.0.113.10
